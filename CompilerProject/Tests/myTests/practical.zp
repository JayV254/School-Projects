int main()
{
    int i, j, rows;

    puts("Enter number of rows: ");
    rows = getnum();

    for(i=1; i<rows+1; i=i+1)
    {
        for(j=1; j<i+1; j=j+1)
        {
            puts("* ");
        }
        puts("\n");
    }
    return 0;
}
